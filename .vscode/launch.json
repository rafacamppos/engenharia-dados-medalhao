{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Ingest Bronze (module)",
      "type": "python",
      "request": "launch",
      "module": "src.ingestion.ingestao",
      "cwd": "${workspaceFolder}",
      "env": {
        "PYTHONPATH": "${workspaceFolder}/src"
      }
    }
  ]
}